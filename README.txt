ESP32 World Clock v5.4-alpha5

See README.md for complete installation, configuration, and operating details.

The touchscreen radar page now shows a vertical stack of cached frame times in
LOOP mode. The oldest time is at the top, the newest is at the bottom, inactive
times are gray, and the currently displayed frame time is yellow. LATEST mode
keeps the left margin clear.
